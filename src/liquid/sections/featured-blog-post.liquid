{%- liquid
  assign options = section.settings
  assign color_scheme = options.color_scheme
  assign section_spacing_top = options.section_spacing_top
  assign section_spacing_bottom = options.section_spacing_bottom
  assign show_author = options.show_author
  assign show_date = options.show_date
  assign show_comment_count = options.show_comment_count
  assign show_read_more = options.show_read_more
  assign show_subheading = options.show_subheading
  assign container_size = options.container_size
  assign enable_background_around_container = options.enable_background_around_container
-%}

{{ 'blog.css' | asset_url | stylesheet_tag }}

{% capture css %}
  {% style %}
    .section-{{section.id}} {
        --gsc-section-spacing-top: {{ section_spacing_top }}px;
        --gsc-section-spacing-bottom: {{ section_spacing_bottom }}px;
    }
  {% endstyle %}
{% endcapture %}

{% render 'inline-css-minifier', css: css %}

<div class="page_title_container">
  <h1 class="page_title" style="color: {{ section.settings.page_title_color }}">
    {{ section.settings.page_title }}
  </h1>
  <h2 class="page_subtitle" style="color: {{ section.settings.page_subtitle_color }}">
    {{ section.settings.page_subtitle }}
  </h2>
</div>

<div class='section-{{ section.id }} section-spacing color-{{ color_scheme }} {% if enable_background_around_container %} featured-articles--color-background  {% endif %}'>
  <div class='featured-articles__container container container--{{ container_size }} {% if container_size == 'full-screen' %} featured-articles__container-full-screen{% endif %}'>
    {%- if section.blocks.size > 0 -%}
      <carousel-component
        class='carousel featured-articles__carousel'
        data-section-id='{{ section.id }}'
        data-animation='fade'
      >
        <div class='embla carousel__viewport featured-articles__carousel-viewport' data-carousel-viewport>
          <div class='carousel__container featured-articles__carousel-container' data-carousel-container>
            {%- for block in section.blocks -%}
              {%- liquid
                assign options = block.settings
                assign article = options.article
                assign image = options.image | default: article.image
                assign title = article.title
                assign excerpt_or_content = article.excerpt_or_content | strip_html
                assign author = article.author
                assign date = article.published_at | date: format: 'abbreviated_date'
                assign comments_count = article.comments_count

                if article == blank
                  assign title = 'sections.featured_blog_posts.default_article_title' | t
                  assign excerpt_or_content = 'sections.featured_blog_posts.default_article_content' | t
                  assign author = 'sections.featured_blog_posts.default_article_author' | t
                  assign date = '11/03/2020'
                  assign comments_count = 0
                endif
              -%}

              <div
                class='featured-article carousel__slide block-{{ block.id }}'
                block-id='{{ block.id }}'
                {{ block.shopify_attributes }}
              >
                <div class='featured-article__details-wrap' style="background-color: {{section.settings.text_bg}}">
                  <div class='featured-article__details'>
                    {%- if section.blocks.size > 1 -%}
                      <h2 class='featured-article__order-number'>
                        {% if forloop.index < 10 %}0{% endif -%}
                        {{- forloop.index }}
                      </h2>
                    {%- endif -%}
                    <div class='featured-article__content'>
                      <div class='featured-article__meta'>
                        {%- if show_author -%}
                          <span class='featured-article__meta-item'>{{ author }}</span>
                        {%- endif -%}
                        {%- if show_date -%}
                          <span class='featured-article__meta-item'>{{ date }}</span>
                        {%- endif -%}
                        {%- if show_comment_count -%}
                          <span class='featured-article__meta-item'>
                            {{- 'sections.featured_blog_posts.comments_count' | t: count: comments_count -}}
                          </span>
                        {%- endif -%}
                      </div>
                      {%- if title != blank -%}
                        <h4 class='featured-article__title' style="color: {{ block.settings.article_title_color }}">
                          <a
                            class='unstyled-link'
                            href='{% if article.url != blank %}{{ article.url }}{% else %}#{% endif %}'
                            aria-label='{{ title }}'
                          >
                            {{- title -}}
                          </a>
                        </h4>
                      {%- endif -%}
                      {%- if show_subheading and excerpt_or_content != blank -%}
                        <div class='featured-article__text' style="color: {{ block.settings.article_text_color }}">{{ excerpt_or_content }}</div>
                      {%- endif -%}
                      {%- if show_read_more -%}
                        <a
                          class='link featured-article__link'
                          href='{% if article.url != blank %}{{ article.url }}{% else %}#{% endif %}'
                          aria-label='{{ 'sections.featured_blog_posts.read_more_link_aria_label' | t: blog_post_title: title }}'
                          style="color: {{ block.settings.read_more_text_color }}"
                        >
                          <span class='link__text'>{{ 'sections.featured_blog_posts.read_more_link' | t }}</span>
                          {% render 'icon', icon_name: 'link-chevron', class: 'link__chevron' %}
                        </a>
                      {%- endif -%}
                    </div>
                  </div>
                </div>
                <a
                  class='unstyled-link featured-article__link zoom-parent-for-hover'
                  href='{% if article.url != blank %}{{ article.url }}{% else %}#{% endif %}'
                  aria-label='{{ 'sections.featured_blog_posts.read_more_link_aria_label' | t: blog_post_title: title }}'
                >
                  {%- if image != blank -%}
                    <img
                      class='image featured-article__image zoom lazyload'
                      {% render 'image-attributes', image: image %}
                    >
                  {%- else -%}
                    {{
                      'image'
                      | placeholder_svg_tag: 'placeholder-image image featured-article__image featured-article__image--placeholder zoom'
                    }}
                  {%- endif -%}
                </a>
              </div>
            {%- endfor -%}
          </div>
        </div>

        {%- if section.blocks.size > 1 -%}
          <div class='featured-article__btns-wrap'>
            <div class='featured-article__btns'>
              <carousel-btn
                class='carousel-btn featured-article__carousel-btn featured-article__carousel-btn--prev'
                data-scroll-direction='prev'
                tabindex='0'
                style="background-color: {{section.settings.carousel_btn_color}}"
              >
                {% render 'icon',
                  icon_name: 'chevron',
                  class: 'carousel-btn__icon featured-article__carousel-btn-chevron x-flip'
                %}
              </carousel-btn>
              <carousel-btn
                class='carousel-btn featured-article__carousel-btn featured-article__carousel-btn--next'
                data-scroll-direction='next'
                tabindex='0'
                style="background-color: {{section.settings.carousel_btn_color}}"
              >
                {% render 'icon',
                  icon_name: 'chevron',
                  class: 'carousel-btn__icon featured-article__carousel-btn-chevron'
                %}
              </carousel-btn>
              {% style %}
                .carousel-btn[disabled] {
                  background-color: {{section.settings.carousel_btn_color}};
                  opacity: 0.7; 
                }
              {% endstyle %}
            </div>
          </div>
        {%- endif -%}
      </carousel-component>
    {%- else -%}
      {{ 'collection-4' | placeholder_svg_tag: 'placeholder-image section-stub' }}
    {%- endif -%}
  </div>
</div>

{% schema %}
{
  "name": "t:sections.featured_blog_posts.name",
  "tag": "section",
  "settings": [
    {
      "type": "select",
      "id": "color_scheme",
      "options": [
        {
          "value": "default",
          "label": "t:sections.general_section_settings.color_scheme_option_default_label"
        },
        {
          "value": "primary",
          "label": "t:sections.general_section_settings.color_scheme_option_primary_label"
        },
        {
          "value": "secondary",
          "label": "t:sections.general_section_settings.color_scheme_option_secondary_label"
        },
        {
          "value": "custom-1",
          "label": "t:sections.general_section_settings.color_scheme_option_custom_1_label"
        },
        {
          "value": "custom-2",
          "label": "t:sections.general_section_settings.color_scheme_option_custom_2_label"
        },
        {
          "value": "custom-3",
          "label": "t:sections.general_section_settings.color_scheme_option_custom_3_label"
        }
      ],
      "default": "custom-1",
      "label": "t:sections.general_section_settings.color_scheme_label",
      "info": "t:sections.general_section_settings.color_scheme_tip"
    },
    {
      "type": "color",
      "id": "text_bg",
      "label": "Text Background Color"
    },
    {
      "type": "text",
      "id": "page_title",
      "label": "Page Title"
    },
    {
      "type": "color",
      "id": "page_title_color",
      "label": "Page Title Color"
    },
    {
      "type": "text",
      "id": "page_subtitle",
      "label": "Page Sub Title"
    },
    {
      "type": "color",
      "id": "page_subtitle_color",
      "label": "Page Sub Title Color"
    },
    {
      "type": "checkbox",
      "id": "enable_background_around_container",
      "default": false,
      "label": "t:sections.general_section_settings.enable_background_around_container_label"
    },
    {
      "type": "select",
      "id": "container_size",
      "options": [
        {
          "value": "full-screen",
          "label": "t:sections.general_section_settings.container_size.screen_width_option"
        },
        {
          "value": "page-width",
          "label": "t:sections.general_section_settings.container_size.page_width_option"
        },
        {
          "value": "large",
          "label": "t:sections.general_section_settings.container_size.large_width_option"
        }
      ],
      "default": "full-screen",
      "label": "t:sections.general_section_settings.container_size.label"
    },
    {
      "type": "range",
      "id": "section_spacing_top",
      "min": 0,
      "max": 100,
      "step": 1,
      "default": 0,
      "unit": "t:sections.general_section_settings.section_spacing_top.unit",
      "label": "t:sections.general_section_settings.section_spacing_top.label"
    },
    {
      "type": "range",
      "id": "section_spacing_bottom",
      "min": 0,
      "max": 100,
      "step": 1,
      "default": 0,
      "unit": "t:sections.general_section_settings.section_spacing_bottom.unit",
      "label": "t:sections.general_section_settings.section_spacing_bottom.label"
    },
    {
      "type": "checkbox",
      "id": "show_author",
      "default": true,
      "label": "t:sections.featured_blog_posts.show_author_label"
    },
    {
      "type": "checkbox",
      "id": "show_date",
      "default": true,
      "label": "t:sections.featured_blog_posts.show_date_label"
    },
    {
      "type": "checkbox",
      "id": "show_comment_count",
      "default": true,
      "label": "t:sections.featured_blog_posts.show_comment_count_label"
    },
    {
      "type": "checkbox",
      "id": "show_subheading",
      "default": true,
      "label": "t:sections.featured_blog_posts.show_subheading_label",
      "info": "t:sections.featured_blog_posts.show_subheading_info"
    },
    {
      "type": "checkbox",
      "id": "show_read_more",
      "default": true,
      "label": "t:sections.featured_blog_posts.show_read_more_label"
    },
    {
      "type": "color",
      "id": "carousel_btn_color",
      "label": "Carousel Button Color"
    }
  ],
  "blocks": [
    {
      "type": "featured_post",
      "name": "t:sections.featured_blog_posts.featured_post_block_name",
      "settings": [
        {
          "type": "article",
          "id": "article",
          "label": "t:sections.featured_blog_posts.featured_post_block_article_label"
        },
        {
          "type": "color",
          "id": "article_title_color",
          "label": "Article Title Color"
        },
        {
          "type": "color",
          "id": "article_text_color",
          "label": "Article Text Color"
        },
        {
          "type": "color",
          "id": "read_more_text_color",
          "label": "Read More Color"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "t:sections.featured_blog_posts.featured_post_block_image_label"
        }
      ]
    }
  ]
}
{% endschema %}
